[project]
name = "fastapi-rpg"
version = "0.1.0"
description = "FastAPI RPG board game"
requires-python = ">=3.8"

dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "sqlalchemy",
]

[tool.uv]
python = "3.11"
